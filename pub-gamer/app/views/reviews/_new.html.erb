<%= form_for [ @venue, @review ] do |f| %>
    <%= render 'reviews/rating' %>
    <%= render 'reviews/vibe' %>
    <%= render 'reviews/week' %>
  <div>
    <%= f.text_area :content, cols: 40, rows: 10, placeholder: "Review here!" %>
  </div>
  <div id="submit-review">
    <%= f.button :submit %>
  </div>
<% end %>